# Examples

Runnable programs, checked by the same type checker as the library itself, so
they cannot quietly drift out of date.

- `post_to_mastodon.py` - register an app, sign someone in, post
- `post_to_many.py` - one post to several networks, handling partial failure
