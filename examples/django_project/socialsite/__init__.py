"""The project package: settings, the root URL map, and the two entry points.

Named after the site rather than `config`, which is the other common Django
convention and the one that collides with every other project in a
repository of examples.
"""
