# Adding a platform

*Coming soon - this will be written alongside the first platform, so the
instructions describe something that actually exists.*

The short version: a platform is one Python class. You can add it to this
repository, or publish your own package that registers itself. socialchimp
finds installed platforms automatically, so your package does not need to be
merged here to work.
